dill==0.4.1
scikit-image==0.26.0
MedPy==0.5.2
nibabel==5.4.2
Pillow==12.2.0
matplotlib==3.11.1
scipy==1.17.1
timm==1.0.28
# torch/torchvision are intentionally NOT exact-pinned: the right build (CPU vs a
# specific CUDA version) depends on the machine. Pin a floor and let the install
# command pick the right wheel - see README "How to Run" / the CI workflow for
# the CPU-only install command used there.
torch>=1.13.0
torchvision>=0.14.0
# Pinned to what this repo's own tests/demo are verified against locally. The
# deployed HF Space is a separate repo/environment pinned to gradio==4.0.0 (its
# sdk_version) - the two are not meant to match, see README "Deployment" section.
gradio==6.20.0
audioop-lts==0.2.2; python_version >= "3.13"
huggingface_hub==1.24.0
kagglehub==1.0.2
pydicom==3.0.2

# RAG grounding layer (Phase 2)
langchain==1.3.14
langchain-community==0.4.2
langchain-huggingface==1.2.2
langchain-groq==1.1.3
langchain-openai==1.3.5
sentence-transformers==5.6.0
faiss-cpu==1.14.3
pytest==9.1.1
