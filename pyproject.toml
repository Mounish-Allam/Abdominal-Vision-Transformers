[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [
    "DataSet",
    "DataSetSample",
    "model",
    "outputs",
    ".venv",
]

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = [
    "E501",  # line length - many long docstrings/prompts predate this config
]
